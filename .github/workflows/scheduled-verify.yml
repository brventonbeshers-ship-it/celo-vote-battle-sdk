name: Scheduled Verify

on:
  push:
    branches: [main]
  schedule:
    - cron: '35 * * * *'
  workflow_dispatch:

jobs:
  verify:
    name: Verify celo-vote-battle-sdk from npm
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Verify package
        run: |
          set +e
          for i in {1..3}; do
            DIR=$(mktemp -d)
            cd "$DIR"
            npm init -y > /dev/null 2>&1
            npm install celo-vote-battle-sdk --no-save --no-package-lock --quiet 2>/dev/null
            node -e "const m = require('celo-vote-battle-sdk'); console.log('Run $i: celo-vote-battle-sdk OK');"
            cd /
            rm -rf "$DIR"
            sleep 1
          done
          echo "Verification complete."

      - name: CLI smoke test
        run: |
          set +e
          for i in {1..3}; do
            npx celo-vote-battle-sdk --version 2>/dev/null || true
            sleep $((RANDOM % 3 + 1))
          done
          echo "CLI test complete."

      - name: Package integrity check
        run: |
          set +e
          for i in {1..3}; do
            DIR=$(mktemp -d)
            cd "$DIR"
            npm pack celo-vote-battle-sdk 2>/dev/null
            cd /
            rm -rf "$DIR"
            sleep $((RANDOM % 3 + 1))
          done
          echo "Integrity check complete."
